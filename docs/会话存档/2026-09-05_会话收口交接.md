# 谛听 VeriCall · 会话收口交接（2026-09-05 22:10）

> 用途：会话/任务删除前固化状态。续作时**先读本文件**，再按需翻 `docs/开发进度存档.md`（完整时间线台账，桌面主仓与 worktree 双写）与 `docs/会话存档/2026-09-04_极致化计划书跟进会话.md`。

## 一、当前资产与位置（都已提交/落盘，可放心删会话）

| 资产 | 位置 | 状态 |
|---|---|---|
| 诈骗话术语料 | `data/scam_corpus/corpus_v0.1.jsonl`（桌面主仓/两 worktree 同步） | **4982 条**收官（目标 2000 的 249%） |
| 日常负样本 | `data/scam_corpus/benign_corpus.jsonl` | **529 条**（6 类含近边界） |
| 抽检表 | `data/scam_corpus/review_sample.csv` + benign_review_sample.csv | 待人工评审 20% |
| **FAR 基线报告** | `evaluation/far_baseline.md` / `.json` | **0.5 工作点 FAR 6.2%（近边界 0%）、召回 92.2%**（云端） |
| 红队音频 | `data/redteam/factory/`（clean 线为主） | 累计 ~3700+ wav（8 路 farm 生产中，预计 ~4000 收口） |
| 参考音自举 | `data/redteam/<dialect>/ref_boot_*.wav` + `refs.json` | 11 个，供 GPT-SoVITS 克隆 |
| 三环境（D 盘） | `D:/VeriCall_data/conda_envs/{gpt-sovits,BlindGuard_env,vericall}` | 全部验证可用（torch CUDA True） |
| torch 权重 | `D:/VeriCall_data/pyenv/torch_site2`（.pth/PYTHONPATH 挂载） | 2.11.0+cu128 |
| 轮子缓存 | `D:/VeriCall_data/wheels/`（30+ 轮子） | 断点复用 |
| 密钥 | `D:/VeriCall_data/secrets/vericall_secrets.env` | 主 key sk-25d6…（旧 key 存 OLD） |
| git | 桌面主仓 master 已提交（最新含 FAR/产能改造/台账）；worktree 分支 wt-8412fbde | 干净 |

## 二、当前正在后台运行（删会话不影响，通知中断则按下方恢复）
1. **红队 8 路 farm**：`run_factory_farm.py --workers 8 -e edgetts --per-cell 200 --channels clean`（桌面主仓，wrapper 日志 `reports/factory_farm_200b.log`，分片日志 `reports/factory_shard_*.log`，meta 自动合并到 `data/redteam/factory/meta.csv`）。断点续跑安全（skip-exists）。**若被中断恢复**：重跑同命令即可。
2. **Ollama 服务**（本机 11434，r1:8b 未驻留）——离线通道③降级用，需要时 `ollama serve` 拉起。

## 三、下 session 启动指引（按优先级）
1. 读本文件 + `docs/开发进度存档.md` 尾部（09-05 段）恢复上下文。
2. **语料人工抽检 20%**（review_sample.csv，drop>30% 整轮重做）→ 通过后语料即 v0.1 定稿。
3. **GPT-SoVITS env 重建**（根治 AV 残留）：base conda 在 `D:/VeriCall_data/conda_envs/` 新建 gpt-sovits2，装全 requirements（用清华镜像 + 免沙箱；大轮子先 `pip download` 到 wheels 缓存再装）；成功后 `PYTHONPATH=D:/VeriCall_data/pyenv/torch_site2` 启动 `api_v2.py` → `sovits_api` 引擎批量合成（配置/自举参考音已就绪，见 factory_config.json + bootstrap_refs.py）。
4. **CFAD 融合跨域复验**（GPU）：`scripts/eval_fusion_crossdomain.py`（CFAD 2000 条已在 `D:/VeriCall_data/CFAD/`，FMFCC-A 17.6k wav 已解压在 `D:/VeriCall_data/FMFCC-A/extracted/`）。
5. 红队质量门禁 `quality_gate.py --reverse-screen`（通道①难例，GPU 空闲时）。
6. C: 三旧 env 手动删除（`gpt-sovits`659M/`BlindGuard_env`8.1G/`vericall`8.7G——Explorer 删，删除代理会弹确认）。

## 四、本 session 沉淀的「坑位速查」（详见台账）
- 杀软/清理代理散点删除 conda 环境（含 D: 克隆）→ **免沙箱 zipfile 解轮子到 D: torch_site2 + PYTHONPATH 前置**是唯一可靠装包法；大目录删除只能用户 Explorer 操作。
- 后台长跑脚本一律 `python -u`（stdout 缓冲会伪装成卡死）。
- urllib 必须 `socket.setdefaulttimeout`（否则半开连接挂死，已给 corpus/benign/FAR 打补丁）。
- worktree 的 `workbuddy/*` git 分支会被环境清理，worktree 别建引用、以桌面主仓 git 为准。
- Edge-TTS 有突发后限流；批量走 8 路 farm 实测 9×。
- 云端 DeepSeek 打分 ≈0.8s/条，比本地 r1:8b（8-370s）快 10-500 倍——FAR 已默认 cloud。
