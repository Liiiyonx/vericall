# 谛听 VeriCall · 开发进度存档

> 本文件是项目的**持续进度台账**：每完成一步实质工作就追加一条，最新进展在最上。
> 与《参赛优化任务书·八、实施记录》的区别：任务书只记里程碑，本文件记每一步（含失败与返工）。

---

## 2026-09-04

### ✅ 已完成

| 事项 | 产出 | 状态 |
|---|---|---|
| **SSL 选型实验出数**（9 月检查点） | `evaluation/ssl_selection.md`：B1 XLS-R 冻结+LR 头 dev EER **1.61%**（24844 条全量，train 2 万）；A0 dev 0.745% / eval 3.494% | ✅ 结论：未达 30% 门槛，**B3 暂不上马**，SSL 特征转「三路融合」复评 |
| **融合复评（计划书 §一.1 验证）** | `evaluation/fusion_stack.md`：AASIST 0.588% / XLS-R+LR 1.607% / **LR stacking 融合 0.354%（dev 24844 全量，相对 -39.8%）**；stacking 权重 aasist=-7.91 ssl=-9.65，等权平均同效 | ✅ **融合方向验证通过**，下一步 CFAD/FMFCC-A 中文集复验；自检告警：AASIST 复测 0.588% vs 核验 0.745%，偏差待查 |
| SSL 脚本四处修复 | 协议文件定位 cm_protocols 子目录；截取前 shuffle（协议按类聚块，bonafide 在前）；按文件大小排序组批+断点续提（`.tmp_ssl/`）；batch 8→4 | ✅ 已验证并提交（20bdc86） |
| A0 基线口径修正 | model_quality.json 单位统一为百分数；dev 同集对比为主口径（此前 eval vs dev 错配导致"假胜出"结论） | ✅ |
| 昨日成果归档提交 | commit 6a2a399（49 文件，P1-1 修复+三管线+文档）；.gitignore 补 `data/redteam/**/*.mp3|flac`（退化信道产物曾误入库，已 amend 清除） | ✅ |
| WavLM-Base+ 权重 | `D:/VeriCall_data/models/wavlm-base-plus`（hf-mirror，B2 备选已就位） | ✅ |
| 测试回归 | 66/66 全绿（vericall 环境） | ✅ |

### 🚨 环境事故：vericall conda 环境被持续破坏（优先处理）

- 现象：`transformers`/`huggingface_hub` 先被卸载；数小时后 **Lib 目录标准库被删至仅剩 22 项**（os.py/encodings 全丢，解释器无法启动）。
- 判断：极可能是**杀毒软件/清理软件在后台持续隔离删除** `C:\Users\Liii\.conda\envs\vericall\` 下的文件（两次静默崩溃也可能与此相关）。
- 处置：**给 Windows Defender/杀软添加排除目录** `C:\Users\Liii\.conda\`（和 `D:\VeriCall_data\`），然后重建 vericall 环境。
- 临时替代：GPU 任务已切换到 `BlindGuard_env`（torch 2.8.0+cu128，已补装 sklearn/soundfile）；融合实验即在该环境完成。
- ⚠ 语料后台进程（PID 49236，vericall python）目前靠已加载进内存的模块续命，**不要重启它**；重启前必须先修好 vericall 环境。

### 🔄 进行中（后台任务）

| 任务 | 目标 | 当前 |
|---|---|---|
| 话术库扩量（--append --per-cell 70 --seed 44） | 累计 2,000+ 条 | 后台运行中（本地 r1:8b ~24s/条，预计 10h+），日志 `reports/followup_0904_stage2.log` |
| 负样本库首批（--per-category 90） | ~540 条 | 排队在话术库之后 |
| 红队反向难例筛选（--reverse-screen --screen-limit 500） | 难例清单 | 排队最后（需 GPU 空闲） |

### ⚠ 风险与待办

- **GPU 单卡 8GB 是硬约束**：GPU 实验（SSL/训练）与 Ollama（r1:8b 常驻 ~6GB VRAM）不能并发，后台任务必须串行排队——今天已发生两次并发 OOM 硬崩溃（无 traceback），靠断点续提救回。
- **安全删除钩子会拦截项目目录内的文件删除**（unlink 抛 OSError SHFileOperationW）——脚本内删除临时文件必须 try/except 兜底。
- vericall 环境的 `transformers`/`huggingface_hub` 今天一度被外部因素卸载（原因不明，已重装 5.16.1 并清理 `~` 前缀残留目录）——若再发生先查 `pip list`。
- 云端 LLM 密钥（SCAM_LLM_*）仍未配置 → 话术库扩量速度受限；配置后提速约 10 倍。
- B2（WavLM 备选）权重已就位，需要时 `--ssl D:/VeriCall_data/models/wavlm-base-plus` 即可跑（特征已缓存机制复用）。

### ⏭ 下一步（按优先级）

1. **修好 vericall 环境**（杀软加排除 → 重建），否则语料进程结束后无法重启；
2. 话术库跑满 2,000 后：人工抽检 20%（`data/scam_corpus/review_sample.csv`），drop 率 >30% 整轮重做；
3. 负样本库首批完成后跑通道③ FAR 基线（近边界样本必审）；
4. **CFAD/FMFCC-A 中文集融合复验**（融合 0.354% 是英文 dev 成绩，中文域才是参赛卖点）；
5. 反向难例筛选结果回填红队质量报告，规划增量训练；
6. 安装 GPT-SoVITS 到 `external/`（克隆线，等志愿者参考音）。

---

## 2026-09-03

### ✅ 已完成

| 事项 | 产出 | 状态 |
|---|---|---|
| 《数据获取方式详册》定稿 | `docs/数据获取方式详册.docx`（15 页，目录页码已重算校准） | ✅ 15/15 页视觉验收通过 |
| 目录页码修复工具 | `scripts/gen_data_guide/fix_toc_pagerefs.py`（可复用） | ✅ 34 条 PAGEREF 与 PDF 逐条核对一致 |
| MLAAD 名称严谨性修正 | 详册中改用中文描述名（不臆造未核实英文名） | ✅ |
| 极致化升级计划书 | `docs/极致化升级计划书.md`（不计算力上限版，北极星指标量化） | ✅ 已评审 |
| 话术库 v0.1 管线 | `scripts/scam_corpus/`（8 类×4 阶段网格、云端/本地双后端、去重、自洽过滤、抽检） | ✅ 首跑 29 条入库 |
| 话术库标注规范 | `docs/话术库标注规范.md`（三轴标签 + 负样本库规范） | ✅ |
| 红队合成工厂 | `scripts/redteam_factory/`（编排/门禁/隔离检查/README） | ✅ 已产 149 条 wav |
| Edge-TTS 引擎接入 | 免 GPU、含方言音色（辽宁/陕西/粤/台），`run_factory.py --engine edgetts` | ✅ 全网格 348 条合成×5 信道=1740 wav 进行中 |
| 质量门禁修复 | 时长阈值 10s→20s（母本非切分段）；read_wav 签名修正 | ✅ 已验证 |
| 工厂断点续跑修复 | 内存管道替代中间 mp3 文件（规避批量删除安全钩子）+ 合成重试×3 | ✅ 已验证（16k/6s 正常） |
| 语料管线增量落盘 | build_corpus 每条接受即写文件（长批次中断不丢数据），此前为末尾一次性写 | ✅ 已验证语法 |
| 隔离检查 | `check_isolation.py` 通过（paths.py 常量定义已白名单） | ✅ |
| 负样本生成器 | `scripts/scam_corpus/build_benign.py`（6 类含 2 类近边界，反向过滤，跨库去重） | ✅ 待首批跑数 |
| SSL 选型实验骨架 | `scripts/eval_ssl_frontend.py` + `evaluation/ssl_selection.md` | ⏸ 待 GPU torch 环境跑数 |
| 志愿者招募启事 | `docs/红队志愿者招募启事.md`（含内部执行备注） | ✅ 待指导老师过目 |
| 环境变量文档更新 | `docs/环境变量.md` 增 SCAM_LLM_* 云端后端说明 | ✅ |

### 🔄 进行中（后台任务）

| 任务 | 目标 | 当前 |
|---|---|---|
| 话术库扩量（seed=43, per-cell 6） | +192 条候选 → 累计约 200 条 | 后台运行中 |
| ~~红队工厂首批（edge-tts 全网格）~~ | 348 合成 × 5 信道 | ✅ **完成：1735 条 wav，物理合格率 95.1%（1650 过 / 85 隔离）** |

### 📦 红队工厂首批产出详情（2026-09-03 16:05 完成）

- **产出**：`data/redteam/factory/` 348 个母本 × 5 信道（clean/phone8k/mp3_16k/amr/noise）= 1735 条 wav + meta.csv（字段对齐详册 §7.2）；
- **覆盖**：6 方言目录（mandarin/sichuan/dongbei/henan/cantonese/minnan）× 多音色 × 29 条话术；
- **质量**：合格率 95.1%；85 条隔离全部为「超长母本」（17 条 53~55s 母本 × 5 信道）——长文本话术所致，非合成失败，后续按详册 §7.2 切分 3~6s 段时可救回；
- **稳定性**：合成重试×3 生效，本轮 0 失败（首轮 NoAudioReceived 全部自愈）；
- **报告**：`quality_report.json` / `quarantine.csv`；
- **待跑**：`quality_gate.py --reverse-screen`（通道①反向难例筛选，需 GPU torch 环境空闲时）。

### ⏭ 下一步（按优先级）

1. 话术库跑满 v0.1 目标 2,000 条（本地 r1:8b ~24s/条，建议配 `SCAM_LLM_*` 切云端提速 10 倍）；
2. 负样本库首批 500 条（Ollama 空闲后跑 `build_benign.py`）；
3. ~~红队工厂首批完成后跑 `quality_gate.py` 出质量报告~~（已完成，见上节；reverse-screen 待 GPU 空闲）；
4. 安装 GPT-SoVITS 到 `external/`（克隆引擎，需志愿者参考音，招募本月内）；
5. GPU 环境装 transformers 后跑 SSL 选型实验出对比数。

### ⚠ 风险与待办

- 云端 LLM 密钥未配置 → 话术库扩量速度受限（本地 8 小时/2k 条）；
- 三套克隆 TTS 未装 + 方言参考音为零 → 红队克隆线待招募；
- SSL 实验需 GPU torch 环境（当前系统 python 为 CPU 版 torch）；
- 详册备份 `docs/数据获取方式详册.docx.bak` 确认无误后可删。

---

## 存档约定

1. 每次实质进展（功能完成/缺陷修复/数据产出/文档交付）立即追加到本文件对应日期下；
2. 数据类产出同步记录条数与路径；代码类记录验证方式；
3. 跨 session 续作时先读本文件恢复上下文。
