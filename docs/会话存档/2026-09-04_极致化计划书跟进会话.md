# 会话存档 · 2026-09-04 晚 · 极致化升级计划书跟进

## 📌 09-05 00:37-02:00 凌晨续更（承接上文）

- **用户已处理**：确认删除 `_GPT-SoVITS.partial`/`.partial2`（✅ 已删，external/ 仅剩 GPT-SoVITS）；手动添加杀软/Defender 排除目录 `C:\Users\Liii\.conda\` + `D:\VeriCall_data\`——**效果已验证**：torch 解包提速 ~30 倍、环境 1 小时零新增空壳。
- **CUDA torch（gpt-sovits env）路线修正**：官方 download.pytorch.org 经沙箱代理龟速弃用 → 阿里云 pytorch-wheels cu128 直链 `--find-links` 下载 `torch/torchaudio==2.11.0+cu128`（cp310 轮存在！此前误判因阿里云 HTML 用 `&#43;` 非 `%2B` 编码）。下载 ~0.5MB/s 慢但稳（沙箱对 curl 大文件限速，pip 自身不受影响），进行中。
- **语料扩量 stage5**：1205→1290 条，约 7 条/分，预计 03:30 前后跑满 2000；**负样本库已并行启动**（build_benign --backend cloud --per-category 90，~540 条，结束才落盘 benign_corpus.jsonl，预计 1.5h）。
- 抽检/合并/冒烟等收尾见上表待办。

> 存档时间：2026-09-04 22:40 · 执行：WorkBuddy（worktree master-8412fbde）
> 关联文档：《极致化升级计划书》（2026-09-03 制定）、《开发进度存档》（同日台账，本 session 所有进展已同步追加）

---

## 一、用户指令原文（按时间序）

1. `@"docs/极致化升级计划书.md" 帮我跟进`
2. `每做完一部分就要记得给我在本地存档`
3. `sk-****6c25 帮我在本地记住`（DeepSeek API Key，明文已按约定脱敏；真身存 `D:\VeriCall_data\secrets\vericall_secrets.env`）
4. `你在本地记住时别把敏感信息和账户有关的东西放C盘啊`
5. `当前做的这些任务 以及对话帮我在本地存档`

## 二、跟进动作与结果

| # | 动作 | 结果 |
|---|---|---|
| 1 | 对照计划书 2026.09 下检查点盘点现状 | SSL 选型报告 ✅ 已出（B1 dev 1.61%，B3 不上马）；融合 stacking dev 0.354% ✅ 超额；语料 v0.1 目标 2k — 发现仅 274 条且进程已死 |
| 2 | 救活话术库扩量 | Ollama 服务已停 → 重启 `ollama serve`；stage4 断点续跑（本地 r1:8b，274→325 条） |
| 3 | 配置 DeepSeek 云端后端 | 密钥存 **D 盘** secrets 文件（遵守"敏感信息不放 C 盘"）；API 连通验证通过；扩量切 `--backend cloud`（stage5），条目 `source=cloud:deepseek-chat` 已核实，速度 ~4 条/分 |
| 4 | 踩坑记录 | ① secrets 文件变量必须 `export`（首轮 KeyError 空跑 2240 格，未污染语料）；② 三个检出 `.gitignore` 补 `.env` 屏蔽 |
| 5 | GPT-SoVITS 环境（红队克隆线，计划书 §四） | conda env `gpt-sovits`（py3.10）建好；torch 2.14.0+torchaudio 2.11.0 装好；requirements 三处 Windows 适配（去 `--no-binary=opencc`、pyopenjtalk→prebuilt、删 jieba_fast）+ `--no-cache-dir` 绕安全删除钩子；依赖安装与 v1/v2 预训练权重（hf-mirror lj1995 仓）后台进行中 |
| 6 | 文档同步 | `docs/环境变量.md` 增"密钥存放约定"；`~/.workbuddy/MEMORY.md` 记用户跨项目偏好（敏感信息不上 C 盘、每步存档） |

## 三、存档时刻的后台任务

| 任务 | 位置 | 日志/产物 |
|---|---|---|
| 话术库扩量 stage5（云端 DeepSeek，目标 2000+） | worktree master-2374103d | `reports/followup_0904_stage5_cloud.log`；语料 `data/scam_corpus/corpus_v0.1.jsonl`（存档时 ~355 条） |
| GPT-SoVITS 依赖安装（--no-cache-dir） | env gpt-sovits | requirements.win.txt |
| GPT-SoVITS v1/v2 预训练权重克隆 | `external/GPT-SoVITS/GPT_SoVITS/pretrained_models/` | hf-mirror lj1995/GPT-SoVITS |

## 四、遗留待办（需用户或后续 session 处理）

1. **Defender/杀软排除目录**（管理员权限手动加）：`C:\Users\Liii\.conda\`、`D:\VeriCall_data\`——不加则 conda 环境随时再被散点删除（09-04 事故根因未除）。
2. `_GPT-SoVITS.partial` / `.partial2` 克隆残骸：**待用户确认后删除**。
3. 语料跑满 2000 后：人工抽检 20%（`review_sample.csv`），drop 率 >30% 整轮重做；之后合并回本检出（append+去重）。
4. 负样本库首批（`build_benign.py --per-category 90`）可走云端后端。
5. CFAD/FMFCC-A 中文集融合复验（参赛卖点，计划书 §一验收线）。
6. 红队志愿者招募启事：待指导老师过目。
7. GPT-SoVITS：依赖装完 → import 冒烟；v3/v4 权重按需补克隆。
